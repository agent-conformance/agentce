# Chokepoint inventory

Generated by `find_chokepoints --repo . --json` (Phase B) and completed by reading low-confidence
sites. One row per chokepoint; record only path, line, kind, and whether an enforcement point sits in
front — never code or content (S-9).

| Path | Line | Kind | Confidence | Enforcement point present | Plan (emit / route / gap) |
|---|---|---|---|---|---|
| <path> | <line> | model_call | high | no | emit ModelCall (self_report) |

Kinds: model_call, tool_call, authz, credential, approval, memory, retrieval, component_load,
decision, incident, instruction_entry, refusal, disclosure. For each item, state whether an enforcement
point already sits in front (route through it) or the event must be emitted agent-side (self_report),
and record any chokepoint with no hook in `docs/agentce/gaps.md` (never simulate it, S-1).

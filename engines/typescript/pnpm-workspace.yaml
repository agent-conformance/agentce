# Biome and esbuild (tsx's bundler) fetch a platform-native binary in a postinstall step; pnpm
# blocks dependency build scripts by default, so allow exactly these two to run theirs.
allowBuilds:
  "@biomejs/biome": true
  esbuild: true

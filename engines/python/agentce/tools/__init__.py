"""Adopter tooling shipped with the engine (SPEC §13.4).

Small, focused utilities an adopter runs directly, separate from the assessment CLI — currently the
applicability-profile validator (:mod:`agentce.tools.validate_profile`).
"""

from __future__ import annotations

__all__: list[str] = []

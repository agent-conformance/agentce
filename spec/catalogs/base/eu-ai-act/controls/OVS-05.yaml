id: OVS-05
version: "2026.09"
title: Consequential decisions record the authorisation that gated them
crosswalk:
  - {framework: eu-ai-act, clause: "Art. 26(2)", relation: implements, verified_against_text: false}
applicability:
  applies_to_roles: [both]
evaluation:
  mode: automated
  rung: 2
  min_source_class: self_report
  minimum_evidence:
    - {event: Decision, class: self_report}
  shape: shapes/OVS-05.ttl
expectations:
  - {id: S1, text: "Consequential decisions record the authorisation that gated them."}
severity: high
evidence_strength: strong
tolerance: {kind: count, max: 0}
tolerance_rationale: "A single OVS-05 exception is a control failure."
false_positive_modes:
  - "A condition declared as an accepted deviation in the applicability profile."
techniques:
  - "techniques/ovs-evidence-at-source"
test_cases:
  - {id: pass, expected: passed, fixture: test/OVS-05/passed.jsonl}
  - {id: fail, expected: failed, fixture: test/OVS-05/failed.jsonl}
  - {id: na, expected: inapplicable, fixture: test/OVS-05/inapplicable.jsonl}

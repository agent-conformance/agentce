id: DAT-01
version: "2026.09"
title: Consequential decisions record the data they consumed
crosswalk:
  - {framework: eu-ai-act, clause: "Art. 10", relation: implements, verified_against_text: false}
interpretations: [IR-06, IR-12]
applicability:
  applies_to_roles: [both]
evaluation:
  mode: automated
  rung: 2
  min_source_class: self_report
  minimum_evidence:
    - {event: Decision, class: self_report}
  shape: shapes/DAT-01.ttl
expectations:
  - {id: S1, text: "Consequential decisions record the data they consumed."}
severity: high
evidence_strength: strong
tolerance: {kind: count, max: 0}
tolerance_rationale: "A single DAT-01 exception is a control failure."
false_positive_modes:
  - "A condition declared as an accepted deviation in the applicability profile."
techniques:
  - "techniques/dat-evidence-at-source"
test_cases:
  - {id: pass, expected: passed, fixture: test/DAT-01/passed.jsonl}
  - {id: fail, expected: failed, fixture: test/DAT-01/failed.jsonl}
  - {id: na, expected: inapplicable, fixture: test/DAT-01/inapplicable.jsonl}

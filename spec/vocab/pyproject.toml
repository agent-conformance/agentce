[project]
name = "agentce-vocab"
version = "0.1.0"
description = "AgentCE evidence vocabulary (PROV-O profile and glue relations) and its validator."
requires-python = ">=3.12"
dependencies = [
  "rdflib>=7,<8",
]

[tool.uv]
package = false

[project]
name = "agentce-tools"
version = "0.1.0"
description = "Repository-level checks: the no-learned-components denylist scanner and the SPDX licensing check."
requires-python = ">=3.12"
dependencies = []

[tool.uv]
package = false
